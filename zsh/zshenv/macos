# vim:ft=zsh

# Set this to read .zshrc from a different path.
# Disabled so that ZSH can work with GHOSTTY
# ZDOTDIR=$HOME/.config/zsh

# sources
[ -f /opt/homebrew/bin/brew ] && eval "$(/opt/homebrew/bin/brew shellenv)"

PATH=/usr/local/bin:$PATH
export PATH

#!/usr/bin/env zsh

local dots=(alacritty kitty xbar)
local submodules=(nvim zsh pass personal)

local stow_folders=($dots $submodules)

for folder in ${stow_folders}; do
  stow --restow --target=$HOME $folder
done
